import planSeed from "../data/plan.seed";

// Vätternrundan starts Friday evening in waves through the night.
// Countdown targets an evening start on race day.
export const RACE_START_ISO = `${planSeed.raceDateISO}T19:00:00`;
export const RACE_DATE_ISO = planSeed.raceDateISO;
export const GOAL_LABEL = "sub 9h";
